name: Publish SDK to npm (DEPRECATED)

# El SDK ahora vive en ZeaCl/soma-sdk.
# El publish se hace desde ese repo, no desde soma/sdk/.
# Este workflow se mantiene como referencia pero no hace nada.

on:
  workflow_dispatch:  # solo manual, nunca automático

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SDK publish is now handled by ZeaCl/soma-sdk"
